  // styl property ordering: https://github.com/rossPatton/stylint/blob/master/src/data/ordering.json

@import 'kouto-swiss'
@import '../../globie-library/helpers'

// ******************************************************

// RESETS

meyer-reset()
box-sizing-reset()
normalize()

// ******************************************************

// BOWER LIBRARY

@import 'library/library'

// ******************************************************

// GRID

@import '../../globie-library/grid'

// ******************************************************

// FONTS

@import 'fonts/fonts'

$font-sans = 'Post Grotesk', font-stack('Helvetica');
$font-serif = 'Stanley', font-stack('Georgia');
$font-logo = 'Druk', font-stack('Helvetica');
$font-ruberta = 'YoungSerif', font-stack('Helvetica');

// ******************************************************

// TYPOGRAPHY

@import '../../globie-library/type'

$font-size-larger = 1.3rem

$font-leading-basic = 1.35
$font-leading-tighter = 1.15
$font-leading-larger = 1.45

$font-tracking-basic = .001em
$font-tracking-wider = .05em

html
  font-family: $font-sans
  letter-spacing: $font-tracking-basic

body
  line-height: $font-leading-basic

.font-sans
  font-family: $font-sans

.font-serif
  font-family: $font-serif

.font-larger
  font-size: $font-size-larger

.line-tighter
  line-height: $font-leading-tighter

.line-larger
  line-height: $font-leading-larger

.font-logo
  font-family: $font-logo

.font-ruberta
  font-family: $font-ruberta

.font-light
  font-weight: 300

// ******************************************************

// VARIABLES

$color-black = rgb(0, 0, 0)
$color-white = rgb(255, 255, 255)
$color-blue = rgb(2, 24, 83)
$color-grey = rgb(248, 248, 248)

// ******************************************************

// BASIC

html
  color: $color-blue
  background-color: $color-grey

img
  height: auto
  max-width: 100%

.mobile-only
  display: initial !important

.desktop-only
  display: none !important

// ******************************************************

// MEDIA QUERIES

$gutter = 14px

$margin-micro = ($gutter / 3)
$margin-tiny = ($gutter / 2)
$margin-small = ($gutter)
$margin-basic = ($gutter * 2)
$margin-mid = ($gutter * 3.5)
$margin-large = ($gutter * 5)

$font-size-basic = 15px
$font-size-extra = 32px
$font-size-large = 28px
$font-size-mid = 21px
$font-size-small = 14px
$font-size-tiny = 13px

grid-maker(96%, $gutter, s)

space-maker($margin-micro, $margin-tiny, $margin-small, $margin-basic, $margin-mid, $margin-large)

font-sizes($font-size-basic, $font-size-extra, $font-size-large, $font-size-mid, $font-size-small, $font-size-tiny)

@import 'responsive/mobile'

@media screen and (min-width: 720px)

  $gutter = 21px

  $margin-micro = ($gutter / 3)
  $margin-tiny = ($gutter / 2)
  $margin-small = ($gutter)
  $margin-basic = ($gutter * 2)
  $margin-mid = ($gutter * 3.5)
  $margin-large = ($gutter * 5)

  $font-size-basic = 20px
  $font-size-extra = 52px
  $font-size-large = 36px
  $font-size-mid = 24px
  $font-size-small = 17px
  $font-size-tiny = 14px

  space-maker($margin-micro, $margin-tiny, $margin-small, $margin-basic, $margin-mid, $margin-large)

  grid-maker(96%, $gutter, m)

  font-sizes($font-size-basic, $font-size-extra, $font-size-large, $font-size-mid, $font-size-small, $font-size-tiny)

  @import 'responsive/tablet-portrait'

@media screen and (min-width: 1024px)

  $gutter = 24px

  $margin-micro = ($gutter / 3)
  $margin-tiny = ($gutter / 2)
  $margin-small = ($gutter)
  $margin-basic = ($gutter * 2)
  $margin-mid = ($gutter * 3.5)
  $margin-large = ($gutter * 5)

  $font-size-basic = 19px
  $font-size-extra = 64px
  $font-size-large = 48px
  $font-size-mid = 24px
  $font-size-small = 17px
  $font-size-tiny = 14px

  grid-maker(1000px, $gutter, l)

  space-maker($margin-micro, $margin-tiny, $margin-small, $margin-basic, $margin-mid, $margin-large)

  font-sizes($font-size-basic, $font-size-extra, $font-size-large, $font-size-mid, $font-size-small, $font-size-tiny)

  .mobile-only
    display: none !important

  .desktop-only
    display: initial !important

  @import 'responsive/tablet-landscape'

@media screen and (min-width: 1280px)

  $font-size-basic = 21px
  $font-size-extra = 64px
  $font-size-large = 48px
  $font-size-mid = 27px
  $font-size-small = 19px
  $font-size-tiny = 16px

  grid-maker(1200px)

  font-sizes($font-size-basic, $font-size-extra, $font-size-large, $font-size-mid, $font-size-small, $font-size-tiny)

  @import 'responsive/laptop'

@media screen and (min-width: 1366px)

  $font-size-basic = 24px
  $font-size-extra = 64px
  $font-size-large = 54px
  $font-size-mid = 30px
  $font-size-small = 21px
  $font-size-tiny = 16px 

  $gutter = 24px

  grid-maker(1300px, $gutter, xl)

  font-sizes($font-size-basic, $font-size-extra, $font-size-large, $font-size-mid, $font-size-small, $font-size-tiny)

  @import 'responsive/desktop'

@media screen and (min-width: 1680px)

  grid-maker(1600px)

  @import 'responsive/hd'

@media screen and (min-width: 1920px)

  grid-maker(1800px)

  @import 'responsive/retina'
